# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python backend
backend/.venv/
backend/**/__pycache__/
backend/.env
backend/contracts/openapi/*.openapi.json

# Uploaded case documents are runtime data, not source code.
storage/

# Deploy secrets (никогда не коммитим прод-секреты)
deploy/aws/.env.prod
deploy/aws/.env.*.local

# AI / IDE — не в репозиторий
.cursor/
.claude/
**/.claude/

# Локальные venv вне backend/.venv
.venv-pir/

# PII / реестры с персональными данными контрагентов — никогда не коммитим.
# Образцы для тестов — только в backend/demo/ и обезличенные.
/*.xlsx
/BIN_IIN_*.xlsx
/Отчет*.xlsx
/реестр*.xlsx
/Юристы*.xlsx
# Большие PIR-источники (3.9MB) — реальные BIN/ИИН/имена. Только в backend/templates/
# допустимы обезличенные шаблоны до 100KB.
backend/data/pir_*.xlsx
backend/templates/pir_*_data*.xlsx
backend/templates/*.OLD-BACKUP.xlsx
backend/templates/pir_source_*.xlsx
